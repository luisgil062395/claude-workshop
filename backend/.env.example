# Copy to backend/.env and fill in your own key. Never commit the real file --
# .env is listed in .gitignore.
#
# Get a key at https://console.anthropic.com/
# Used only by Django, server-side. It is never sent to the browser and never
# appears in an API response.
ANTHROPIC_API_KEY=
