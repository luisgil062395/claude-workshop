DATABASE_URL="file:./dev.db"
ANTHROPIC_API_KEY=""
# Only needed if your Anthropic API key is a personal key not scoped to a
# single workspace (you'll see a 400 error mentioning "identity-linked API
# key" if so). Find the workspace ID at https://platform.claude.com/settings/workspaces
ANTHROPIC_WORKSPACE_ID=""
